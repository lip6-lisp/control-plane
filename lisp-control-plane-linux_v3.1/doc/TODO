Action
======
Change map_entry and mapping_flags to referral_entry and referral_flags for all
the code related to referral.

Doing so would make the code more flexible as referrals and mappings would not
be related at all. For the moment referrals are just mappings but it could
change in the future.

Action
======
Implement syslog instead of console and allow user to set debug level

for the moment, log information is pushed to the console, creating a bottleneck


Action
======
Implement rig
